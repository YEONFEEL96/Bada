<?xml version="1.0" encoding="utf-8"?>
<!--
  Action button for the send-picker radio banner (#290). Same blue
  gradient, ripple and pressed wash as `btn_visibility_on_background`
  (the app's "switched on" surface), but at a 12dp corner radius so the
  button reads as a compact rounded rectangle sitting inside the 16dp
  banner plate rather than a full pill competing with the sheet's
  Cancel button.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#26FFFFFF">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <gradient
                android:angle="135"
                android:endColor="#5B82FF"
                android:startColor="#2D58D5"
                android:type="linear" />
        </shape>
    </item>
    <item>
        <selector>
            <item android:state_pressed="true">
                <shape android:shape="rectangle">
                    <solid android:color="#33FFFFFF" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
